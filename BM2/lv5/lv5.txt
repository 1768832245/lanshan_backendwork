5.1	一个切片的修改会影响其他切片、并发访问（使用mutex锁上应该可以？）
5.2	如a := []int{1,2,3,4,5},可用a = a[:4]  ，从索引的0开始到索引3，最终a为{1，2，3，4}（可运用到栈LIFO中）
5.3	散列函数，一种明文到密文的映射（不太理解嘿嘿）
5.4	复制（返回对象值的拷贝），原数值应该会丢弃？
5.5	container包中list是一个双向链表，element是链表中元素结构（用结构体实现）
5.6	如Error Fail等可标记测试状态和测试结果，再细分为继不继续向下执行
	skip等可跳过当前测试，也可决定是否记录格式化跳过原因、继不继续向下执行
	log等记录日志信息
	Setenv设置环境变量
	Name返回测试名称
	TempDir返回临时目录，用于测试的临时文件
	